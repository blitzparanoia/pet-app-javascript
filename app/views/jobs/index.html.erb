<h1> Job Listings </h1>

<%= "#{top_company.name} currently has the most open positions!" %>
<br>
<br>
<%= link_to 'Add New Job', new_job_path %>

<br><br>

<% @jobs.each do |job| %>
Date Created:<%= job.created_at.strftime("%b %d, %Y") %><br>
Open Position:<%= link_to(job) do %>
<%= job.title %><br>
<% end %>
Company:<%= job.company.name %><br>
Job Tlsype: <%= job.position %><br>
Description: <%= job.description %>

<br><br><br>
<% end %>
